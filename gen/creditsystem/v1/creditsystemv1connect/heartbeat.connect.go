// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: creditsystem/v1/heartbeat.proto

package creditsystemv1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/cshubhamrao/cloud-credit-system/gen/creditsystem/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// HeartbeatServiceName is the fully-qualified name of the HeartbeatService service.
	HeartbeatServiceName = "creditsystem.v1.HeartbeatService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// HeartbeatServiceHeartbeatStreamProcedure is the fully-qualified name of the HeartbeatService's
	// HeartbeatStream RPC.
	HeartbeatServiceHeartbeatStreamProcedure = "/creditsystem.v1.HeartbeatService/HeartbeatStream"
	// HeartbeatServiceHeartbeatProcedure is the fully-qualified name of the HeartbeatService's
	// Heartbeat RPC.
	HeartbeatServiceHeartbeatProcedure = "/creditsystem.v1.HeartbeatService/Heartbeat"
)

// HeartbeatServiceClient is a client for the creditsystem.v1.HeartbeatService service.
type HeartbeatServiceClient interface {
	// Primary: bidirectional stream between workload cluster agent and control plane.
	// The send direction carries heartbeat metrics; the receive direction carries
	// async ACKs, quota status, and enforcement commands.
	HeartbeatStream(context.Context) *connect.BidiStreamForClient[v1.HeartbeatRequest, v1.HeartbeatResponse]
	// Fallback: unary for environments that cannot maintain streaming connections.
	// Commands piggyback on the response at the cost of up to one heartbeat interval
	// of additional enforcement latency.
	Heartbeat(context.Context, *connect.Request[v1.HeartbeatRequest]) (*connect.Response[v1.HeartbeatResponse], error)
}

// NewHeartbeatServiceClient constructs a client for the creditsystem.v1.HeartbeatService service.
// By default, it uses the Connect protocol with the binary Protobuf Codec, asks for gzipped
// responses, and sends uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the
// connect.WithGRPC() or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewHeartbeatServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) HeartbeatServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	heartbeatServiceMethods := v1.File_creditsystem_v1_heartbeat_proto.Services().ByName("HeartbeatService").Methods()
	return &heartbeatServiceClient{
		heartbeatStream: connect.NewClient[v1.HeartbeatRequest, v1.HeartbeatResponse](
			httpClient,
			baseURL+HeartbeatServiceHeartbeatStreamProcedure,
			connect.WithSchema(heartbeatServiceMethods.ByName("HeartbeatStream")),
			connect.WithClientOptions(opts...),
		),
		heartbeat: connect.NewClient[v1.HeartbeatRequest, v1.HeartbeatResponse](
			httpClient,
			baseURL+HeartbeatServiceHeartbeatProcedure,
			connect.WithSchema(heartbeatServiceMethods.ByName("Heartbeat")),
			connect.WithClientOptions(opts...),
		),
	}
}

// heartbeatServiceClient implements HeartbeatServiceClient.
type heartbeatServiceClient struct {
	heartbeatStream *connect.Client[v1.HeartbeatRequest, v1.HeartbeatResponse]
	heartbeat       *connect.Client[v1.HeartbeatRequest, v1.HeartbeatResponse]
}

// HeartbeatStream calls creditsystem.v1.HeartbeatService.HeartbeatStream.
func (c *heartbeatServiceClient) HeartbeatStream(ctx context.Context) *connect.BidiStreamForClient[v1.HeartbeatRequest, v1.HeartbeatResponse] {
	return c.heartbeatStream.CallBidiStream(ctx)
}

// Heartbeat calls creditsystem.v1.HeartbeatService.Heartbeat.
func (c *heartbeatServiceClient) Heartbeat(ctx context.Context, req *connect.Request[v1.HeartbeatRequest]) (*connect.Response[v1.HeartbeatResponse], error) {
	return c.heartbeat.CallUnary(ctx, req)
}

// HeartbeatServiceHandler is an implementation of the creditsystem.v1.HeartbeatService service.
type HeartbeatServiceHandler interface {
	// Primary: bidirectional stream between workload cluster agent and control plane.
	// The send direction carries heartbeat metrics; the receive direction carries
	// async ACKs, quota status, and enforcement commands.
	HeartbeatStream(context.Context, *connect.BidiStream[v1.HeartbeatRequest, v1.HeartbeatResponse]) error
	// Fallback: unary for environments that cannot maintain streaming connections.
	// Commands piggyback on the response at the cost of up to one heartbeat interval
	// of additional enforcement latency.
	Heartbeat(context.Context, *connect.Request[v1.HeartbeatRequest]) (*connect.Response[v1.HeartbeatResponse], error)
}

// NewHeartbeatServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewHeartbeatServiceHandler(svc HeartbeatServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	heartbeatServiceMethods := v1.File_creditsystem_v1_heartbeat_proto.Services().ByName("HeartbeatService").Methods()
	heartbeatServiceHeartbeatStreamHandler := connect.NewBidiStreamHandler(
		HeartbeatServiceHeartbeatStreamProcedure,
		svc.HeartbeatStream,
		connect.WithSchema(heartbeatServiceMethods.ByName("HeartbeatStream")),
		connect.WithHandlerOptions(opts...),
	)
	heartbeatServiceHeartbeatHandler := connect.NewUnaryHandler(
		HeartbeatServiceHeartbeatProcedure,
		svc.Heartbeat,
		connect.WithSchema(heartbeatServiceMethods.ByName("Heartbeat")),
		connect.WithHandlerOptions(opts...),
	)
	return "/creditsystem.v1.HeartbeatService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case HeartbeatServiceHeartbeatStreamProcedure:
			heartbeatServiceHeartbeatStreamHandler.ServeHTTP(w, r)
		case HeartbeatServiceHeartbeatProcedure:
			heartbeatServiceHeartbeatHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedHeartbeatServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedHeartbeatServiceHandler struct{}

func (UnimplementedHeartbeatServiceHandler) HeartbeatStream(context.Context, *connect.BidiStream[v1.HeartbeatRequest, v1.HeartbeatResponse]) error {
	return connect.NewError(connect.CodeUnimplemented, errors.New("creditsystem.v1.HeartbeatService.HeartbeatStream is not implemented"))
}

func (UnimplementedHeartbeatServiceHandler) Heartbeat(context.Context, *connect.Request[v1.HeartbeatRequest]) (*connect.Response[v1.HeartbeatResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("creditsystem.v1.HeartbeatService.Heartbeat is not implemented"))
}
